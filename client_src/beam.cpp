#include "beam.h"

Beam::Beam(PlatformSnapshot pltfrm, MatchSurfaces& surfaces, SDL2pp::Renderer& renderer){
    if (pltfrm.type) {
        if (pltfrm.angle) {
            /* code */
        }
        
    }
    this->x = 
}